fastapi>=0.110
uvicorn[standard]>=0.29
pydantic>=2.6
python-dotenv>=1.0
httpx>=0.27
SQLAlchemy>=2.0
# For Postgres (set DATABASE_URL=postgresql+psycopg://...), also install:
#   psycopg[binary]>=3.1
# LLM provider SDKs (optional at runtime — imported lazily per provider)
anthropic>=0.49
openai>=1.30
